# Docker Environment Configuration
# 
# Used when running step-ci in Docker Compose test environment

env:
  BASE_HOST: http://api:3000
  API_HOST: http://api:3000/api
  TIMEOUT: 10000
  VERBOSE: false

